package com.app.planify.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light ────────────────────────────────────────────────────────────────────
val PlPrimaryLight             = Color(0xFF4A7C59) // Verde Sage de la UI
val PlOnPrimaryLight           = Color(0xFFFFFFFF)
val PlPrimaryContainerLight    = Color(0xFFE9EBE6) // Fondo de botones secundarios/pestañas
val PlOnPrimaryContainerLight  = Color(0xFF1A1A1A)
//#FEF9F3
val PlBackgroundLight          = Color(0xFFF5EEE4) // Crema/hueso suave de fondo general
val PlSurfaceLight             = Color(0xFFFFFFFF) // Blanco puro para las tarjetas (cards)
val PlOnBackgroundLight        = Color(0xFF1A1A1A) // Texto principal casi negro
val PlOnSurfaceLight           = Color(0xFF1A1A1A)
val PlOutlineLight             = Color(0xFF7A7A7A) // Gris medio para bordes o texto secundario

val PlErrorLight               = Color(0xFFBA1A1A)
val PlOnErrorLight             = Color(0xFFFFFFFF)

// ── Dark ─────────────────────────────────────────────────────────────────────
val PlPrimaryDark              = Color(0xFF70B797) // Verde más brillante y eléctrico para contraste
val PlOnPrimaryDark            = Color(0xFF121414)
val PlPrimaryContainerDark     = Color(0xFF2D3131) // Contenedores secundarios oscuros
val PlOnPrimaryContainerDark   = Color(0xFFFFFFFF)

val PlBackgroundDark           = Color(0xFF121414) // Negro mate con sutil toque frío
val PlSurfaceDark              = Color(0xFF1C1E1E) // Gris oscuro para las tarjetas (cards)
val PlOnBackgroundDark         = Color(0xFFFFFFFF) // Texto principal blanco puro
val PlOnSurfaceDark            = Color(0xFFFFFFFF)
val PlOutlineDark              = Color(0xFF8A9292) // Gris claro/azulado para texto secundario

val PlErrorDark                = Color(0xFFFFB4AB)
val PlOnErrorDark              = Color(0xFF690005)